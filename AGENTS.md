# Mega UI 작업 지침

- UI를 생성·수정하기 전에 `docs/ai-guide.md`, `docs/ux-writing.md`와 해당 컴포넌트 API를 읽고 반드시 준수한다.
- UX 라이팅은 디자인 계약이다. 기본 문구·예제·오류·버튼·접근 가능한 이름에 동일하게 적용한다.
- 컴포넌트와 문구는 실제 동작을 먼저 확인한 뒤 선택한다. 구현에 없는 성공·원인·혜택·복구를 주장하지 않는다.
- UI 작업 완료 전 `npm run lint:ux`를 실행하고, `docs/ux-writing.md`의 의미 검토를 수행한다. 위반이 남으면 완료로 보고하지 않는다.
- 전체 변경 검증은 `npm run check`를 사용한다. 완료 보고에 검사 결과와 문맥 검토 결과, 확인하지 못한 사항을 남긴다.
- 검사 통과를 위해 금지 문구를 분할하거나 검사를 비활성화하지 않는다. 불가피한 원문 보존 예외는 경로·문구·사유를 한정해 리뷰한다.
