# 150개 확장 검증 기록

2026-09-09 · 로컬 개발 서버 `http://127.0.0.1:5173/` · Chrome.

## 자동 검사

`npm run check` 통과: TypeScript, Prettier, 라이브러리 빌드, Node 테스트 31개, 문서 사이트 빌드.

- 요청 목록 150개 이름의 export 존재, 19개 별칭의 동일 구현 재사용
- SSR 결과의 네이티브 의미, 라벨, disabled/controlled 상태, HTML 이스케이프
- 시맨틱 토큰·디자인 계약, NaN/Infinity/뒤집힌 범위의 안전한 출력
- QR 경로·UTF-8 입력 구분·용량 초과 처리
- 표 정렬·집계·가상화 경계, 스프레드시트 열 라벨·텍스트 커서 이동

문서 사이트의 메인 JS는 약 502 KB(압축 전)여서 Vite의 500 KB 청크 권고 경고가 있습니다. 예제와 원문 소스를 함께 싣는 카탈로그 번들이며 빌드는 성공합니다. 라이브러리 ESM은 약 133 KB(압축 전)입니다.

## 실제 브라우저 확인

| 영역          | 확인한 동작                                                                                                                                        |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 기초·레이아웃 | 기존/확장 화면 렌더, Portal 열고 닫기, SplitPane 방향키로 50→51% 변경                                                                              |
| 콘텐츠        | Carousel 1→2 슬라이드 이동, 이미지 모달과 확대 컨트롤 렌더, QR 표시                                                                                |
| 파일          | 카탈로그 내 메모리 샘플 File로 blob 이미지 미리보기 렌더                                                                                           |
| AI            | Enter 채팅 전송, 입력 초기화, 비동기 전송 실패 알림과 초안 보존, 재시도 성공                                                                       |
| 표·업무       | 검색과 검색 해제, 금액 정렬, 상태/스프레드시트 셀 편집, 트리 접기, 행 선택, 칸반 열 이동                                                           |
| 가상화        | End 키로 스크롤 후 마지막 993–1000행 렌더 확인; 전체 1,000행 DOM 생성 없이 표시                                                                    |
| 달력·시트     | 날짜 선택, 방향키 날짜 포커스 이동, 셀 내부 커서 이동과 경계에서 다음 셀 이동                                                                      |
| 입력          | 비밀번호 표시 전환, 휴대폰 마스크, 범위 슬라이더 방향키, 라디오 방향키, 중첩 TreeSelect 선택                                                       |
| 오버레이      | ContextMenu Shift+F10→ArrowDown→Escape, 트리거 포커스 복원; Popover 열기/Escape; CommandPalette 검색/방향키/실행; AlertDialog 역할; Tour 다음/완료 |
| 테마·레이아웃 | 데스크톱 라이트/다크, 좁은 모바일 뷰포트(실제 CSS 354px)에서 업무 화면·달력 선택과 가로 넘침 없음 확인                                             |

## 검증 한계

브라우저 확장의 파일 URL 접근 권한이 꺼져 있어 실제 로컬 파일 선택 자동화는 제한됐습니다. 파일 선택 대화상자 호출까지 확인했고, FilePreview는 메모리 샘플로 확인했습니다. 실제 PDF 파일 렌더·업로드 드롭은 수동 확인이 남아 있습니다. 서버 업로드는 컴포넌트 범위가 아닙니다.

스크린 리더, Safari/Firefox, 모든 props 조합·상태의 시각 회귀를 전수 검증한 것은 아닙니다. 복잡한 컴포넌트의 제공 기능과 제외 범위는 각 API 문서를 기준으로 합니다.
